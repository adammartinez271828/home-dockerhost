# secrets / per-service env (keep the .example templates)
.env
env.d/*.env
!env.d/*.env.example

# service data / bind mounts
data
etc-dnsmasq.d
etc-pihole
mediafiles
postgresql
postgresql.bak
backups
dashy_conf.yml
# SmokePing/Speedtest runtime data (RRDs, SQLite, generated config). The
# tracked smokeping/Targets is the source of truth — see `make smokeping-targets`.
smokeping/config
smokeping/data
speedtest/config
